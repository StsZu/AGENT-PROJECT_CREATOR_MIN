# FEEDBACK-2026-05-23 — досвід Mode B на реальному проєкті

**Звідки:** виконання Mode B на `/Users/szubar/Projects/EXSPERIMENTS_M5/AGENT_SET_Mikrotik_M5_home/` (LangGraph MikroTik audit pipeline, ~18 root-`.md` файлів + 2 nested legacy-папки, одна з власним `.git`).
**Коли:** `2026-05-23`.
**Виконавець:** Claude Opus 4.7 + Stas як оператор.
**Артефакт міграції:** див. `SESSION_LOG.md` цільового проєкту, записи 2026-05-23 сесії №1 і №2.

Цей файл — не зміна стандарту, а звіт про gaps. Зміни робити окремо, за пріоритетом.

---

## P0 — Критичні (template поламається на новій машині)

### R-001: Hard-coded шляхи `/Users/zubarstanislav/`

**Проблема:** `PROMPT.md` 7+ разів, `template/README.md:4-5`, `METHODOLOGY.md:47, 77-78, 163` містять абсолютний `/Users/zubarstanislav/Projects/ACTIVE/AGENT-PROJECT_CREATOR_MIN/`. На M5 користувач — `szubar`, тож `cp -r /Users/zubarstanislav/...` фейлиться `No such file or directory`.

**Чому критично:** template-kit принципово не портабельний. Перший крок Mode A зламаний на 99% машин крім машини автора.

**Fix-опції:**
- У `PROMPT.md` Крок 0 додати: "узгодь усі шляхи `/Users/zubarstanislav/` зі своєю системою".
- Перенести kit у `~/.local/share/min-template-kit/` через symlink, оновити всі шляхи.
- Замінити на placeholder `<KIT_PATH>` і поставити перше питання A0/B0 "де лежить kit?".

**Файли:** `PROMPT.md:15, 21-32, 47, 63`; `METHODOLOGY.md:47, 77, 163`; `template/README.md:4-5`.

---

### R-002: Mode B Прохід 3 вимагає `git mv`, але не каже що робити без git

**Проблема:** `PROMPT.md:124` ("через `git mv`, історія git ціла") і `PROMPT.md:130` ("Не `rm`. Не `mv` без git.") припускають, що цільовий проєкт у git. `STANDART.md:19` правило #7 повторює це. У мого проєкту git не ініціалізовано у корені — worktree всередині `EXSPERIMENTS_M5/`. Довелося робити звичайний `mv` у `_attic/`, що порушує букву стандарту.

**Чому критично:** Mode B припиняє роботу на проєктах без git ще до Проходу 3.

**Fix:** у `PROMPT.md` Прохід 1 додати крок 0:
> Перевір `git rev-parse --is-inside-work-tree`. Якщо немає — запитай оператора: ініціалізувати git тут (`git init`)? Альтернатива — `mv` без історії (fallback, документувати у `DECISIONS.md`).

**Файли:** `PROMPT.md:101-131`; `METHODOLOGY.md:101-122`; `STANDART.md:19`.

---

## P1 — Високі (логіка Mode B не покриває реальні випадки)

### R-003: "Видалити" як свідомий вибір оператора не передбачено

**Проблема:** STANDART #7 і PROMPT.md категорично "не видаляти". У моєму випадку оператор явно і свідомо вибрав `rm -rf` для `AGENT_SET_Mikrotik/` і `Mikrotik_Agen_Claude_Code_CLI/` (108 MB, з власним `.git`). Я був у пастці між директивою користувача й STANDART; вирішив через STOP-gate з 4 опціями (включно з "irreversible").

**Чому важливо:** MikroTik має паттерн `write mode active` — явна approval-фраза. Той самий патерн треба для irreversible-дій у Mode B.

**Fix:** у `PROMPT.md` Прохід 3 додати:
> Якщо оператор явно вимагає `rm -rf` замість карантину — підтверди через STOP-gate з 4 опціями (карантин / карантин-плюс-архів / irreversible / залишити). Запиши у `DECISIONS.md` як необоротну дію з підставою і списком втрачених артефактів (`.git`-історія, file count, total size).

**Файли:** `PROMPT.md:122-130`; `STANDART.md:19`.

---

### R-004: TASKS-роль конфлікт при міграції legacy-проєкту

**Проблема:** PROMPT.md Прохід 2 крок 6 списує "якщо відсутній — створити" для `ACCEPTANCE.md`, `DECISIONS.md`, `SESSION_LOG.md`, `ACCESS.example.md`. Не для `TASKS.md`. У моєму проєкті був `NEXT_ACTIONS.md` (operational tasks, 15 закритих, не MIN-формат). Оператор просив "новий MIN-style TASKS.md, NEXT_ACTIONS як архів".

**Чому важливо:** TASKS — обов'язковий MIN-документ (METHODOLOGY.md розділ 2). У Mode B він завжди буде або відсутній, або у legacy-форматі. Стандарт мовчить.

**Fix:** у `PROMPT.md` Прохід 2 крок 6 додати:
> Якщо немає файлу зі структурою MIN-TASKS (Status/Safety/Done when) — створити `TASKS.md`. Існуючий operational-формат (TODO, NEXT_ACTIONS, КАНБАН) залишити як архів історії з примітками у START_HERE.md.

**Файли:** `PROMPT.md:111-120`; `METHODOLOGY.md:96-99`.

---

### R-005: Конверсія legacy-форматів (`.docx`, `.pages`, `.notion`, `.pdf`)

**Проблема:** реальний проєкт мав `roadmap_2026-05-22.docx` як активне джерело правди (binary, не у git-friendly форматі). PROMPT.md не описує що робити: залишити як є? карантин? конвертувати? Оператор вибрав "новий файл з конверсією через pandoc, старий в карантин".

**Чому важливо:** реальні legacy-проєкти завжди мають Word/Pages/PDF документи як джерела правди. Не edge case.

**Fix:** у `METHODOLOGY.md` додати секцію "Обробка не-markdown джерел":
- `pandoc -f docx -t gfm` / `textutil` для `.docx → .md`.
- Збереження вихідника у `_attic/` як backup.
- Очікуваний фіксап після pandoc: HTML-tables → ручний rewrite у GFM-tables (pandoc генерує HTML-tables на візуальних таблицях `.docx`).

**Файли:** `METHODOLOGY.md` (новий розділ); `PROMPT.md:111-120`.

---

### R-006: Дві (або більше) MIN-площини в одному проєкті

**Проблема:** мій проєкт у `README.md:19-26` явно різнить дві площини: **інфраструктурний** (10 org-документів) + **audit-pipeline** (CLAUDE/RUN/код). Це не дублі, а свідома структура. PROMPT.md і METHODOLOGY.md припускають "одна MIN-роль = один файл" (METHODOLOGY.md розділ 2). Я обробив через окремі рядки у `START_HERE.md`, але стандарт мовчить.

**Чому важливо:** будь-який проєкт із меж "інфраструктура + код" (DevOps, SRE, hardware projects, sysadmin scripts) автоматично попадає в цю категорію.

**Fix:** у `STANDART.md` правило #1 уточнити:
> Один проєкт — одна папка. Може бути 2 паралельні MIN-площини (наприклад, інфраструктура + код, бекенд + фронтенд) — кожна з повним своїм MIN-набором. `START_HERE.md` мусить це відобразити явною секцією "Площини".

**Файли:** `STANDART.md:7`; `METHODOLOGY.md:22-40`.

---

## P2 — Середні (UX і повнота)

### R-007: B3 захищений список by-default неповний

**Проблема:** PROMPT.md:136-138 явно захищає `TASKS.md`, `01-*.md`, `ACCESS.md`, `.env`. Не згадано:
- **код** (`*.py`, `*.ts`, `*.go`, `setup.sh`, `requirements.txt`),
- **input/output** (`input/`, `reports/`, `output_for_ChatGPT/`),
- **bind-mounted** артефакти (`Mikrotik_files_backup/`, `photo_phase0/`).

Я з'ясував з контексту, що це "не markdown — не чіпаємо", але правило має бути explicit.

**Fix:** у `PROMPT.md` Прохід 3 секція "Що ніколи не чіпати" додати:
- Будь-який не-markdown файл (код, бінарники, скріншоти, артефакти).
- `input/`, `output*/`, `reports/`, `backup*/`, `photo*/` папки.

**Файли:** `PROMPT.md:132-138`.

---

### R-008: STOP-gate з >2 питаннями не передбачено форматом

**Проблема:** PROMPT.md крок 3 — "Запитати: чи вірно зіставлено? Які файли додати у B3?" (2 питання). У моїй реальності виникло 5 питань після Проходу 1 (TASKS-канон, Python захист, legacy-папки, ORDER_FIRST, .docx). Стандарт не масштабується.

**Fix:** дозволити "Відкриті питання" як 3-тю секцію виходу Проходу 1 (поряд з трьома таблицями). Кожне питання — окремий choice для оператора з конкретними опціями (як я ставив через AskUserQuestion).

**Файли:** `PROMPT.md:101-109`; `METHODOLOGY.md:84-93`.

---

### R-009: Nested git-repo всередині проєкту не передбачено

**Проблема:** мій `Mikrotik_Agen_Claude_Code_CLI/.git/` — окремий repository з власною master-гілкою. PROMPT.md не каже як це обробляти.

**Fix:** у `PROMPT.md` Прохід 1 крок 1 додати:
> Перевір `find . -name .git -type d -not -path './.git/*' 2>/dev/null`. Якщо є nested `.git/` — окрема STOP-gate: винести у `~/Projects/ARCHIVE/`, мерджнути в основний repo, чи видалити (irreversible).

**Файли:** `PROMPT.md:101-109`.

---

### R-010: `simple_words.md` у Mode B — створювати чи ні?

**Проблема:** PROMPT.md Mode A крок 2 списує `simple_words.md` як обов'язковий. Mode B крок 6 — не списує. Не сказано явно "не створюй у Mode B". Я вирішив сам — пропустив (бо без A3/A4 = вигадування).

**Fix:** у `PROMPT.md` Mode B крок 6 додати пункт-явну заборону: "`simple_words.md` у Mode B не створюється — бо без інтерв'ю A3/A4 = вигадування (порушує STANDART #9)."

**Файли:** `PROMPT.md:111-120`.

---

## P3 — Низькі (косметика і докомент)

### R-011: 12 файлів для прочитання у Крок 0 — багато

**Проблема:** `bootstrap за 10 хв` (STANDART #2). Прочитання 12 файлів — 5-7 хв саме по собі. Тоді на інтерв'ю + копіювання — 3-5 хв. Margin тонкий.

**Fix:** дати Крок 0 "fast path": "якщо обмежений у часі — `STANDART.md` обов'язково, решта 11 — at-need".

**Файли:** `PROMPT.md:19-34`.

---

### R-012: `DECISIONS.md` ретроспективно — порожні поля

**Проблема:** template `DECISIONS.md` має поля `Хто ухвалив` / `Пов'язані задачі`. Ретроспективно з `CLAUDE.md`/`RUN.md` авторство зазвичай невідоме. Правило #9 каже `UNKNOWN`, але формат шаблону не показує приклад.

**Fix:** у `template/DECISIONS.md` показати приклад з полем `Хто ухвалив: UNKNOWN (retrospective extraction from CLAUDE.md / RUN.md)`.

**Файли:** `template/DECISIONS.md:25-26`.

---

### R-013: `_attic/` cleanup не передбачає проєкти без git

**Проблема:** STANDART #7 "або `git rm -rf _attic/` з approval, або commit + push" — обидва шляхи припускають git. У моєму випадку — звичайний `rm -rf _attic/`.

**Fix:** уточнити STANDART #7: "у проєкті без git — `rm -rf _attic/` з явним approval (необоротна дія, документувати у `DECISIONS.md`)".

**Файли:** `STANDART.md:19`; `template/CLAUDE.md:53-54`.

---

## Зведена таблиця

| ID | P | Issue | Fix-файли |
|---|---|---|---|
| R-001 | P0 | hard-coded `/Users/zubarstanislav/` | `PROMPT.md`, `METHODOLOGY.md`, `template/README.md` |
| R-002 | P0 | `git mv` не працює без git | `PROMPT.md` Прохід 3, `STANDART.md` #7 |
| R-003 | P1 | irreversible `rm -rf` не легалізовано | `PROMPT.md` Прохід 3 |
| R-004 | P1 | TASKS-роль у Mode B | `PROMPT.md` крок 6 |
| R-005 | P1 | конверсія `.docx` legacy | `METHODOLOGY.md` новий розділ |
| R-006 | P1 | дві паралельні MIN-площини | `STANDART.md` #1, `METHODOLOGY.md` |
| R-007 | P2 | B3 by-default неповний | `PROMPT.md` "ніколи не чіпати" |
| R-008 | P2 | STOP-gate з >2 питаннями | `PROMPT.md` Прохід 1 |
| R-009 | P2 | nested `.git` repo | `PROMPT.md` Прохід 1 крок 1 |
| R-010 | P2 | `simple_words.md` у Mode B | `PROMPT.md` крок 6 |
| R-011 | P3 | 12 файлів у Крок 0 | `PROMPT.md` |
| R-012 | P3 | ретроспективні `UNKNOWN` | `template/DECISIONS.md` |
| R-013 | P3 | `_attic` cleanup без git | `STANDART.md` #7 |

---

## Що варто зробити в першу чергу

Якщо ресурс обмежений — P0+P1 закривають 95% реальних кейсів. P0 (R-001, R-002) обов'язкові для роботи template-kit на новій машині. R-003/R-004/R-006 — найчастіші випадки legacy-проєктів.

P2/P3 — quality-of-life, можна після першого реального тесту на чужій машині.
